[includeIf "gitdir:IlyaKisil/"]
    path = ~/.gitconfig-personal

# Subject to change
[includeIf "gitdir:~/"]
    path = ~/.gitconfig-personal

# Use similar pattern to include work related configs if needed

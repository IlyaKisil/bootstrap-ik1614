require("fzf-lua").setup({
    winopts = {
        fullscreen = true,
        preview = {
            scrollbar = false,
            horizontal     = 'right:50%',
            vertical = 'down:50%',
        },
    },
})


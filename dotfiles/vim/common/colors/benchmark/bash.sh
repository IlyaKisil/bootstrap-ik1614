#!/usr/bin/env bash

# TODO:
# todo:
set -euo pipefail

#Sample comment . risent
let "a=16 << 2";
let 'a=16 << 2';
b="Sample text";

function foo() {
  if [[ $string1 == $string2 || $hello == $bla ]]; then
    for url in `cat example.txt`; do
      curl $url > result.html
    done
  fi
  ()
}

bla() {
  for VARIABLE in 1 2 3 4 5 .. N
  do
      command1
  done
}

|| &&



rm -f $(find / -name core) &> /dev/null
mkdir -p "${AGENT_USER_HOME_${PLATFORM}}"
foo \
  -p \
  -p bla \
  --param bla \
  --param=bla \
  --param="bla" \

curl sr;sr

multiline='first line
           second line
           third line'
cat << EOF
 Sample text
EOF


- link:
    ~/.config/kitty/kitty.conf: dotfiles/kitty/kitty.conf
# - shell:
#     # Executed only if this file doesn't exist (to prevent overriding)
#     - ["[ -f ~/.config/kitty/kitty-local.conf ] && echo '=> Already exists, skipping' || cp dotfiles/kitty/kitty-local.conf ~/.config/kitty/kitty-local.conf", Initialising .kitty-local]
